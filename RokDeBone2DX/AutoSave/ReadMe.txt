このフォルダにはRokDeBone2が自動保存したファイルが作成されます。
現バージョンではモーションウインドウの「全削除」をしたときに
「save日時.qua」が自動保存されます。
また「影響度の再計算をしますか」で「はい」を選択した場合に
「save日時.sig」が自動保存されます。

このフォルダの中身は必要がなければ消してしまって構いませんが、
この「AutoSave」フォルダ自体は消してしまうとRokDeBone2動作時にエラーが起こりますので
注意してください。

